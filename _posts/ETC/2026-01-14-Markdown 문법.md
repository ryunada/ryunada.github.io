---
title : "Markdown 문법"
categories:
    - ETC
date: 2026-01-14
toc: true
toc_label: "Concept"
toc_sticky: True
# toc_icon: ""
sidebar:
  nav: 'counts'
math: true           # 수식 설정
---

|기호|Code|기호|Code|
|:-:|:--|:-:|:--|
|$A \over B$|A \over B|$\partial$|\partial|
|$\cdot$|\cdot|$\dots$|\dots|
|$\times$|\times|$\sigma$|\sigma|
|$\eta$|\eta|$\epsilon$|\epsilon|
|$sqrt$|\sqrt|$\mu$|\mu|
|$\sim$|\sim|$\tilde{x}$|\tilde{x}|
|$\lfloor x \rfloor$|\lfloor x \rfloor|
|$\gamma$|\gamma|$\theta$|\theta|
|$\nabla$|\nabla|$\phi$|\phi|
|$\Rightarrow$|\Rightarrow|||

# 📐 Markdown 수식 기호 정리 (Math Cheat Sheet)

---

## ✏️ 수식 작성 기본 규칙

- 문장 안 수식: `$ ... $`
- 독립 수식: `$$ ... $$`

문장 예시: `$x^2 + y^2 = z^2$`

블록 예시:
$$
x^2 + y^2 = z^2
$$

---

## 1️⃣ 기본 연산 기호 (Basic Operators)

| 기호 | 의미 | Code |
|:-:|---|:--|
| $+$ | 덧셈 | `+` |
| $-$ | 뺄셈 | `-` |
| $\cdot$ | 곱셈(내적) | `\cdot` |
| $\times$ | 곱셈 | `\times` |
| $\div$ | 나눗셈 | `\div` |
| $A \over B$ | 분수 | `A \over B` |
| $\sqrt{x}$ | 제곱근 | `\sqrt{x}` |
| $\sqrt[n]{x}$ | n제곱근 | `\sqrt[n]{x}` |
| $\lfloor x \rfloor$ | 바닥 함수 | `\lfloor x \rfloor` |
| $\lceil x \rceil$ | 천장 함수 | `\lceil x \rceil` |

---

## 2️⃣ 관계 / 비교 기호 (Relations)

| 기호 | 의미 | Code |
|:-:|---|:--|
| $=$ | 같다 | `=` |
| $\neq$ | 같지 않음 | `\neq` |
| $<$ | 작다 | `<` |
| $>$ | 크다 | `>` |
| $\le$ | 작거나 같다 | `\le` |
| $\ge$ | 크거나 같다 | `\ge` |
| $\sim$ | 분포 / 근사 | `\sim` |
| $\approx$ | 근사 | `\approx` |
| $\propto$ | 비례 | `\propto` |

---

## 3️⃣ 그리스 문자 (Greek Letters)

| 기호 | Code |
|:-:|:--|
| $\alpha$ | `\alpha` |
| $\beta$ | `\beta` |
| $\gamma$ | `\gamma` |
| $\delta$ | `\delta` |
| $\epsilon$ | `\epsilon` |
| $\eta$ | `\eta` |
| $\theta$ | `\theta` |
| $\lambda$ | `\lambda` |
| $\mu$ | `\mu` |
| $\sigma$ | `\sigma` |
| $\phi$ | `\phi` |
| $\omega$ | `\omega` |

---

## 4️⃣ 미적분 / 벡터 기호 (Calculus & Vector)

| 기호 | Code |
|:-:|:--|
| $\partial$ | `\partial` |
| $\nabla$ | `\nabla` |
| $\int$ | `\int` |
| $\sum$ | `\sum` |
| $\prod$ | `\prod` |
| $\lim$ | `\lim` |
| $\cdots$ | `\cdots` |
| $\dots$ | `\dots` |

---

## 5️⃣ 장식 기호 (Decorators)

| 기호 | Code |
|:-:|:--|
| $\tilde{x}$ | `\tilde{x}` |
| $\hat{x}$ | `\hat{x}` |
| $\bar{x}$ | `\bar{x}` |
| $\vec{x}$ | `\vec{x}` |
| $\dot{x}$ | `\dot{x}` |

---

## 6️⃣ 논리 / 집합 기호 (Logic & Set)

| 기호 | Code |
|:-:|:--|
| $\Rightarrow$ | `\Rightarrow` |
| $\Leftarrow$ | `\Leftarrow` |
| $\Leftrightarrow$ | `\Leftrightarrow` |
| $\forall$ | `\forall` |
| $\exists$ | `\exists` |
| $\in$ | `\in` |
| $\notin$ | `\notin` |
| $\subset$ | `\subset` |
| $\cup$ | `\cup` |
| $\cap$ | `\cap` |

---

## 7️⃣ 예제 수식

### 평균
$$
\mu = \frac{1}{n}\sum_{i=1}^{n} x_i
$$

### 분산
$$
\sigma^2 = \frac{1}{n}\sum_{i=1}^{n}(x_i - \mu)^2
$$

### 정규분포
$$
X \sim \mathcal{N}(\mu, \sigma^2)
$$

### MSE Loss
$$
L = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2
$$

# 📄 논문 수식 전용 Markdown Cheat Sheet

---

## 1. 논문 기본 수식 표기

- 인라인 수식: `$ ... $`
- 블록 수식: `$$ ... $$`

---

## 2. 논리 기호 (Logic)

| 기호 | Code |
|:-:|:--|
| $\forall$ | `\forall` |
| $\exists$ | `\exists` |
| $\Rightarrow$ | `\Rightarrow` |
| $\Leftarrow$ | `\Leftarrow` |
| $\Leftrightarrow$ | `\Leftrightarrow` |
| $\iff$ | `\iff` |

---

## 3. 집합 기호 (Set Theory)

| 기호 | Code |
|:-:|:--|
| $\in$ | `\in` |
| $\notin$ | `\notin` |
| $\subset$ | `\subset` |
| $\subseteq$ | `\subseteq` |
| $\cup$ | `\cup` |
| $\cap$ | `\cap` |
| $\emptyset$ | `\emptyset` |

---

## 4. 극한 · 합 · 적분

| 기호 | Code |
|:-:|:--|
| $\lim_{x \to \infty}$ | `\lim_{x \to \infty}` |
| $\sum_{i=1}^{n}$ | `\sum_{i=1}^{n}` |
| $\prod_{i=1}^{n}$ | `\prod_{i=1}^{n}` |
| $\int_a^b$ | `\int_a^b` |

---

## 5. 확률 / 통계

| 기호 | Code |
|:-:|:--|
| $P(A)$ | `P(A)` |
| $P(A\mid B)$ | `P(A \mid B)` |
| $\mathbb{E}[X]$ | `\mathbb{E}[X]` |
| $\mathrm{Var}(X)$ | `\mathrm{Var}(X)` |
| $X \sim \mathcal{D}$ | `X \sim \mathcal{D}` |

---

## 6. 논문 예제 수식

### 조건부 확률
$$
P(A \mid B) = \frac{P(A \cap B)}{P(B)}
$$

### 기대값
$$
\mathbb{E}[X] = \sum_x x P(X=x)
$$

### 극한 정의
$$
\lim_{n \to \infty} \frac{1}{n}\sum_{i=1}^n x_i
$$


# 🤖 머신러닝 · 딥러닝 수식 Cheat Sheet

---

## 1. 모델 파라미터 표기

| 기호 | Code |
|:-:|:--|
| $\theta$ | `\theta` |
| $\hat{y}$ | `\hat{y}` |
| $\tilde{x}$ | `\tilde{x}` |
| $\mathbf{x}$ | `\mathbf{x}` |
| $\mathbf{W}$ | `\mathbf{W}` |

---

## 2. 손실 함수 (Loss Functions)

### MSE
$$
L = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2
$$

### MAE
$$
L = \frac{1}{n}\sum_{i=1}^{n}|y_i - \hat{y}_i|
$$

### Cross Entropy
$$
L = -\sum_{i} y_i \log(\hat{y}_i)
$$

---

## 3. 확률 모델

### 정규분포
$$
X \sim \mathcal{N}(\mu, \sigma^2)
$$

### Softmax
$$
\text{softmax}(z_i) = \frac{e^{z_i}}{\sum_j e^{z_j}}
$$

---

## 4. 미분 / 최적화

| 기호 | Code |
|:-:|:--|
| $\nabla_\theta L$ | `\nabla_\theta L` |
| $\partial L / \partial w$ | `\partial L / \partial w` |

### Gradient Descent
$$
\theta_{t+1} = \theta_t - \eta \nabla_\theta L
$$

---

## 5. 정규화 (Regularization)

### L2 Regularization
$$
L = L_0 + \lambda \|\theta\|_2^2
$$

---

## 6. 딥러닝 예제

### 선형 회귀
$$
\hat{y} = \mathbf{W}x + b
$$

### 시그모이드
$$
\sigma(x) = \frac{1}{1 + e^{-x}}
$$

### ReLU
$$
\text{ReLU}(x) = \max(0, x)
$$
